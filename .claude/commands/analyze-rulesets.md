Run the analyze-rulesets skill for client: $ARGUMENTS

Read and follow the skill instructions in .claude/skills/analyze-rulesets/SKILL.md

If no client is specified, ask which client. Pass "all" to analyze all clients.
